version: "3.9"

services:
  proxy:
    image: j3ydocker/j3y-proxy
    volumes:
      - ./docker/volumes/proxy/etc/nginx/sites-enabled:/etc/nginx/sites-enabled
    ports:
      - 80:80
      - 443:443
    command: nginx -g "daemon off;"
